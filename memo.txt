'use cache';
これは先頭ではなく、取ってくる箇所だけつけるほうがいい。先頭だとすべてキャッシュされるため。
あまり変わらない情報とかにつけるようにする


componentのテストでparamsがあるときの書き方が大切
<Qiita params={[~]} />と記載をする

今回はitems.map(item)としているところもあるため、<Qiita items={[{}]}としている